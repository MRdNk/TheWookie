var pg = require('pg')

function postgres_wookie (opts, cb) {
  var that = this

  var opts = opts || {}
  var connstring = require('./db_connstring.js')(opts)

  var client = pg.Client(connstring)
  client.connect()
  
}

module.exports = postgres_wookie;
